name = "recognize-chinese-characters-api"
main = "src/index.js"
compatibility_date = "2024-01-01"

# 环境变量（通过 wrangler secret 设置）
# wrangler secret put PAYPAL_SANDBOX_CLIENT_ID
# wrangler secret put PAYPAL_SANDBOX_SECRET
# wrangler secret put PAYPAL_LIVE_CLIENT_ID
# wrangler secret put PAYPAL_LIVE_SECRET
# wrangler secret put PAYPAL_MODE

[vars]
ALLOWED_ORIGINS = "https://recognize-chinese-characters.online,http://localhost:5173"
