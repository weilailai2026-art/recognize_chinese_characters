# Cloudflare Pages 已配置自动构建，此 workflow 暂时禁用
# 如需启用 GitHub Actions 部署，取消注释并配置 Secrets
name: Disabled

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use Cloudflare Pages auto-deploy instead"
